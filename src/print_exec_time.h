/* Header file for "print_exec_time.c". Max Besley. April 2022.
*/

#include "structs.h"


/* Fulfils Task 2. */
void print_exec_time(proc_t **processes, int n_procs);

/* Accepts an integer array as input and returns the frequency of the most common element. */
int find_freq_of_mode(int *A, size_t size);
